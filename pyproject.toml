[project]
name = "loan-approval-prediction-model"
version = "0.1.0"
description = "Loan Approval Prediction System using Streamlit"
dependencies = [
    "streamlit",
    "pandas",
    "numpy",
    "scikit-learn",
]

[project.scripts]
app = "streamlit:main"

[tool.setuptools]
py-modules = ["app"]
